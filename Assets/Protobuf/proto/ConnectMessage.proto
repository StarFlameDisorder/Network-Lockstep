syntax = "proto3";

package ConnectMessage;

//服务器

message ServerConnectMessage{
    oneof content {
        HandShakeResponse handShakeMessage = 1;
    }
}

//握手（TCP 连接建立后服务端回复，分配 clientId）
message HandShakeResponse{
    string content = 1;
    uint32 clientId = 2;//仅用于tcp回复
}
