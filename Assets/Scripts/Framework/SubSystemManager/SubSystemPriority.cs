namespace Framework
{
    public enum SubSystemPriority
    {
        SystemManager = int.MinValue,
        GameServer = -200000,
        NetWorkManager = -114514,
        TimerManager = -120,
        DataProxyManager = -100,

    }
}
