# 服务器重构

> 创建于 2026-07-25 | 当前服务器代码分析 + KCP 重写目标架构

---

## 一、当前架构概览

```
GameServer (SubSystemBase)                    ← 顶层编排
  ├── ServerNetworkDispatcher                  ← 消息路由 + 客户端身份
  │     ├── TcpServer                          ← TCP: 黏包处理 + 客户端连接
  │     └── UdpServer                          ← UDP: 手写 SEQ/ACK + 重排序 + 重传
  └── RoomManager                              ← 房间 + 帧同步 + 心跳 + 快照 + 重连

客户端映射（三表）:
  _clientsById[clientId] → ClientState          主表
  _tcpToClientId[TcpClient] → clientId          TCP → clientId
  _udpToClientId[IPEndPoint] → clientId         UDP → clientId
```

事件管线：
```
Client → TcpServer/UdpServer → Dispatcher → RoomManager
RoomManager → (OnSendTcp/OnSendUdp) → Dispatcher → TcpServer/UdpServer → Client
```

---

## 二、当前问题清单

### 🔴 [必须改]

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 1 | **`HeartbeatTimeoutSec` 配置被丢弃** | [RoomManager.cs#L83-L86](Assets/Scripts/Network/Server/RoomManager.cs#L83-L86) | `Initialize(heartbeatTimeoutSec)` 参数未赋值给字段，始终使用硬编码 `_heartbeatTimeoutMs = 4000f`，[ServerConfig](Assets/Scripts/Network/Server/ServerConfig.cs#L20) 中的配置形同虚设 |
| 2 | **大厅阶段无心跳检测** | [RoomManager.cs#L90](Assets/Scripts/Network/Server/RoomManager.cs#L90) | `_broadcastTimer` 在 `Start()` 中创建但未启动（仅 `StartRoom()` 启动），JoinRoom 后、游戏开始前无超时机制，断联客户端资源永不释放 |
| 3 | **UDP 端点变更后无法重新绑定** | [BindUdp#L329](Assets/Scripts/Network/Server/ServerNetworkDispatcher.cs#L329) | `c.UdpEndPoint == null` 条件阻止覆盖旧端点。NAT 重绑等场景下新包能找到 clientId 但发送仍走旧端点。`BindTcp` 同问题 |
| 4 | **心跳接收与检测存在线程竞态** | [ReceiveHeartBeat](Assets/Scripts/Network/Server/RoomManager.cs#L271-L277) vs [BroadcastGameSync#L300-L312](Assets/Scripts/Network/Server/RoomManager.cs#L300-L312) | `ActiveTime`/`Online` 在 UDP 接收线程和 Timer 线程间无同步保护，刚恢复的心跳可能被误判超时 |
| 5 | **EndRoom 不清理玩家数据** | [EndRoom#L215-L220](Assets/Scripts/Network/Server/RoomManager.cs#L215-L220) | 仅停止定时器，不清理 `_players`/`_playerByClient`，不触发 `OnRemoveClient` 释放 Dispatcher 中的客户端资源 |
| 6 | **UDP 重传逻辑被注释掉** | [CheckAndResend#L276-L278](Assets/Scripts/Network/Server/UdpServer.cs#L276-L278) | `_socket.Send(pkt.SendData, ...)` 被注释，仅记录日志不实际重传。可靠UDP的重传能力丢失 |
| 7 | **Heartbeat 心跳字段未使用** | [RoomManager.cs#L89-L92](Assets/Scripts/Network/Server/RoomManager.cs#L89-L92) | `HeartBeat` 消息中的 `ClientSendTime`/`ServerProcessTime` 等字段在 `ReceiveHeartBeat` 中完全忽略，只更新了 `ActiveTime` |

### 🟡 [推荐改]

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 8 | **DeleteClient 内部触发 TcpServer.DisconnectClient 造成重入** | [DeleteClient#L346](Assets/Scripts/Network/Server/ServerNetworkDispatcher.cs#L346) | `_tcpServer.DisconnectClient` → `OnClientDisconnected` → `HandleTcpDisconnected` → 同一 `lock(_lock)` 下重入。虽 Monitor 可重入不死锁，但逻辑隐晦 |
| 9 | **PlayerSession.InputQueue 无容量上限** | [InputQueue#L25](Assets/Scripts/Network/Server/RoomManager.cs#L25) | 若某客户端高频发送输入而其他客户端滞后，队列可无限增长 |
| 10 | **GetPlayerIdByName 每次 O(n) 遍历** | [GetPlayerIdByName#L429-L436](Assets/Scripts/Network/Server/RoomManager.cs#L429-L436) | 快照和重连流程中多次调用，缺少 name→playerId 的索引 |
| 11 | **HandleTcpDisconnected 不检查 _clientsById** | [HandleTcpDisconnected#L159](Assets/Scripts/Network/Server/ServerNetworkDispatcher.cs#L159) | `TryGetValue` 可能失败（client 已被 DeleteClient 移除），但事件仍触发 |
| 12 | **TcpServer.Send 无流控** | [TcpServer.cs#L171-L193](Assets/Scripts/Network/Server/TcpServer.cs#L171-L193) | 直接 `stream.Write` 无发送缓冲区控制，大量帧广播时可能阻塞或丢包 |
| 13 | **连接后到加入房间之间无超时** | [HandleTcpConnected](Assets/Scripts/Network/Server/ServerNetworkDispatcher.cs#L121-L150) | 客户端 TCP 连接分配 clientId 后，若永远不发送 JoinRoom，该 clientId 永久占用 |

### 🔵 [可选改]

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 14 | TryGetClientId 的 fallback 路径不安全 | [TryGetClientId#L282-L286](Assets/Scripts/Network/Server/ServerNetworkDispatcher.cs#L282-L286) | 未绑定 TcpClient 时通过 `msgClientId`（来自客户端消息，可能伪造）查找，存在身份冒用风险 |
| 15 | UdpServer 重连不清理旧序号 | [CleanClient](Assets/Scripts/Network/Server/UdpServer.cs#L295-L305) | 清理时删除 `_udpIndex`，下次重连从 0 开始，客户端可能因旧序号缓存导致序号混乱 |

---

## 三、KCP 重写后的目标架构

### 3.1 组件变化

```
现状:                                     KCP方案:
TcpServer ──→ 控制消息(可靠)              KcpServer ──→ 所有消息(可靠有序)
UdpServer ──→ 游戏同步(手工可靠)    →     基于 UdpClient，每个客户端一个 KCP conversation
UdpServer ──→ 心跳                        同一个 conv 多路复用

可以砍掉:
  ✗ TcpServer.cs           ← KCP 提供可靠通道，不再需要 TCP
  ✗ UdpServer.cs 的        ← SEQ/ACK/重排序/重传全部由 KCP 内置
    SEQ/ACK/重传逻辑
  ✗ _tcpToClientId         ← 只剩 conv→clientId 单映射
    _udpToClientId

必须保留:
  ✓ RoomManager 核心       ← 房间生命周期、帧同步模型、快照、玩家会话
  ✓ GameServer             ← 顶层编排
  ✓ ServerConfig           ← 扩展 KCP 参数(nodelay/interval/resend/nc等)
  ✓ 严格锁步模型            ← 不变：等所有在线玩家输入 → 分配帧号 → 广播
```

### 3.2 简化后架构

```
GameServer (SubSystemBase)
  ├── KcpNetworkDispatcher                    ← 简化版，单一传输出口
  │     └── KcpServer                          ← KCP 封装层(UdpClient + ikcp)
  │           └── 每个客户端 = 一个 conv id = clientId
  └── RoomManager                              ← 核心逻辑不变

客户端侧:
  KcpTransport : INetworkTransport             ← 已有接口预留
    └── KcpClient (UdpClient + ikcp)
```

### 3.3 设计决策（待确认）

| 决策点 | 选项A | 选项B | 建议 |
|---|---|---|---|
| 单conv vs 多conv | 所有消息走同一个conv | 控制/游戏同步分两个conv | **单conv**，KCP内部流控防止互相阻塞 |
| 初始握手 | 纯UDP(先发裸包注册再切KCP) | 保留最小TCP(握手后断开) | **保留最小TCP**，一次握手交换conv id后断开，简单可靠 |
| KCP update 循环 | 独立定时器(10ms) | 挂在帧广播定时器 | **独立10ms定时器**，KCP需要比游戏帧率更快的update |
| 心跳 | 仅应用层心跳(RoomManager) | 应用层+KCP层双层心跳 | **应用层心跳保留在RoomManager**，KCP自带的心跳作为辅助 |

---

## 四、KCP C# 库选型讨论

### 候选库对比

> 待补充讨论结论。
