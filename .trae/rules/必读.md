先看README.md文件和Docs目录下的文件，完成任务时记得更新文档(比如TODO.md)，文档不需要复杂(有核心内容即可,就agent和我看，所以能看懂就行)

Debug.Log时前缀必须标明是[Server]还是[Client]和所属的类名

别删任何会导致可读性下降的注释，包括类注释、方法注释、属性注释等
如果注释内容为空，则可以删除

记得提醒我提交git以及提交内容(我手动提交)，之前老是一大堆更新堆一块才提交

还有多提问，比如不确定是否要更新文档时

目前项目仅考虑实现网络同步这一个核心目标，架构不要搭太大，保持简单

unity mcp建议仅执行unity相关的代码，非相关代码(比如沙盒不允许执行的代码)必须让我审核后执行

重要架构和功能写到文档里